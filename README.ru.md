# Huge JSON Viewer

> Открывайте и ищите в **очень больших JSON-файлах (2–3 ГБ и больше)** на **macOS** за секунды — **бесплатный просмотрщик больших JSON с открытым исходным кодом, альтернатива Dadroit**. Когда текстовый редактор или браузер падает на большом JSON-файле, эта программа открывает его мгновенно.

[English](README.md) · [简体中文](README.zh-CN.md) · [日本語](README.ja.md) · [Español](README.es.md) · [Português](README.pt-BR.md) · [Deutsch](README.de.md) · [Français](README.fr.md) · **Русский** · [हिन्दी](README.hi.md) · [العربية](README.ar.md) · [Türkçe](README.tr.md) · [Bahasa Indonesia](README.id.md)

[![Release](https://img.shields.io/github/v/release/bandusix/huge-json-viewer?color=0a6cff)](https://github.com/bandusix/huge-json-viewer/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/bandusix/huge-json-viewer/total?color=28c840)](https://github.com/bandusix/huge-json-viewer/releases)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
![Platform](https://img.shields.io/badge/macOS-Apple%20Silicon%20%2B%20Intel-black?logo=apple)

### [⬇️ Скачать последний DMG](https://github.com/bandusix/huge-json-viewer/releases/latest)

![Huge JSON Viewer — просмотрщик больших JSON для Mac](https://cdn.jsdelivr.net/gh/bandusix/huge-json-viewer@main/docs/screenshot-dark.png)

<details><summary>Светлая тема</summary>

![Huge JSON Viewer — светлая тема](https://cdn.jsdelivr.net/gh/bandusix/huge-json-viewer@main/docs/screenshot-light.png)

</details>

## Зачем это нужно

Большинство текстовых редакторов и онлайн-просмотрщиков JSON **падают или зависают, когда вы пытаетесь открыть большой JSON-файл**, потому что разбирают его целиком в память — файл на 2–3 ГБ раздувается до 15–30 ГБ ОЗУ. **Huge JSON Viewer** так не делает никогда. Он отображает файл в память (memory-map), за один потоковый проход строит компактный индекс и рисует только те строки, что видны на экране. Поэтому он **открывает многогигабайтный JSON за секунды** и мгновенно ищет по всему файлу, оставаясь в пределах ~1,5–2× размера файла по ОЗУ.

Если вы искали *«как открыть большой JSON файл»*, *«JSON файл слишком большой, чтобы открыть»* или **бесплатную альтернативу Dadroit для Mac** — это сделано именно для таких задач.

## Возможности

- ⚡ **Открывает JSON на 2–3 ГБ примерно за 3 секунды** — отображение в память, потоковый индекс, виртуализированное дерево
- 🔍 **Поиск по ключам и значениям** — с учётом регистра или без, обычный текст или **regex**, живой счётчик совпадений, переход к следующему/предыдущему с раскрытием пути
- 🌳 **Сворачиваемое дерево с подсветкой синтаксиса** — номера строк, направляющие отступов, цвета типов и счётчики дочерних элементов (в стиле Dadroit / jsonviewer.app)
- 📄 Открывает `.json`, `.ndjson` / `.jsonl` (определяются автоматически), `.geojson`, `.txt`
- 🖱️ Перетаскивание файлов, ⌘O для открытия, ⌘F для поиска, полная навигация с клавиатуры
- 🌍 **Интерфейс на 20 языках**, с поддержкой письма справа налево (арабский, урду, панджаби)
- 🖥️ **Универсальная сборка** — Apple Silicon (M1–M4) *и* Intel
- 🔒 **100% офлайн** — без загрузки на сервер, без сервера, без телеметрии · приложение 2 МБ
- 🆓 **Бесплатно и с открытым исходным кодом** (MIT)

## Установка

1. **[Скачайте последний `.dmg`](https://github.com/bandusix/huge-json-viewer/releases/latest)** и откройте его.
2. Перетащите **Huge JSON Viewer** в папку «Программы».
3. Первый запуск: приложение не подписано, поэтому **щёлкните по нему правой кнопкой → «Открыть»** и подтвердите (нужно сделать только один раз).

Требования: macOS 11 (Big Sur) или новее. Работает с JSON-файлами до 4 ГБ.

## Как это работает

JSON-файл на 2–3 ГБ невозможно разобрать в объекты в памяти. Вместо этого ядро на Rust:

1. **Отображает файл в память** (`memmap2`) — страницы подгружаются ОС по требованию, а не хранятся в куче.
2. **Выполняет один потоковый проход токенизатора**, чтобы построить компактный плоский индекс (~23 байта на узел JSON) из смещений в байтах и структуры — без разбора в объекты.
3. **Рисует только видимые строки.** Дерево полностью виртуализировано; раскрытие/сворачивание изменяют список видимых строк, а не материализуют весь документ. **Масштабированная полоса прокрутки** позволяет прокручивать миллионы строк, обходя ограничение браузера на высоту элемента.
4. **Ищет по сырым байтам** — SIMD-поиск подстроки / regex поверх mmap с сопоставлением каждого совпадения с его узлом.

Собрано на **Tauri v2** (бэкенд на Rust + веб-фронтенд), упаковано в `.dmg` размером ~2 МБ.

## Huge JSON Viewer в сравнении с другими инструментами для больших JSON

| | Huge JSON Viewer | Dadroit | Текстовые редакторы (VS Code и др.) |
| --- | --- | --- | --- |
| Цена | **Бесплатно и с открытым кодом (MIT)** | Бесплатно + платный Pro | Бесплатно |
| Открывает JSON на 2–3 ГБ | ✅ ~3 с | ✅ | ❌ падает / зависает |
| ОЗУ для файла на 3 ГБ | **~1,5–2×** | мало | часто нехватка памяти |
| Поиск по ключам **и** значениям | ✅ regex | ✅ | ограниченно |
| Нативный macOS (Apple Silicon + Intel) | ✅ универсальный | ✅ | ✅ |
| Языки интерфейса | **20 (с поддержкой RTL)** | немного | много |
| Офлайн / без телеметрии | ✅ | ✅ | ✅ |

## Языки

Интерфейс поставляется на **20 локалях**, переключаемых кнопкой 🌐 (выбор сохраняется, при первом запуске определяется автоматически). Локали с письмом справа налево зеркалят интерфейс, но дерево JSON остаётся слева направо; числа форматируются по локали.

`en-US` · `zh-CN` · `hi-IN` · `es-ES` · `fr-FR` · `ar-EG` · `bn-BD` · `ru-RU` · `pt-BR` · `id-ID` · `ur-PK` · `de-DE` · `ja-JP` · `sw-TZ` · `mr-IN` · `te-IN` · `pa-PK` · `zh-WUU` · `ta-IN` · `tr-TR`

## Сборка из исходников

```bash
npm install
npm run tauri dev                      # hot-reloading dev app
npm run tauri build -- --bundles dmg   # build the DMG
cd src-tauri && cargo test             # engine tests (serde_json oracle, escapes, NDJSON, search)
```

## Ограничения (v1)

- **Размер файла:** до 4 ГБ (компактные смещения `u32`). Файлы больше отклоняются с понятным сообщением.
- **ОЗУ:** индекс занимает ~23 байта на узел, поэтому файлу на 2–3 ГБ нужно примерно **1,5–2× размера файла** по ОЗУ (сам отображённый в память файл — вытесняемый кэш страниц ОС). Mac с 16 ГБ комфортно справляется с файлами на 2–3 ГБ.
- **Поиск** сопоставляет сырые байты файла (экранированный символ совпадает в своей экранированной форме); поиск без учёта регистра работает только для ASCII.

## Лицензия

[MIT](LICENSE) © bandusix

<sub>Ключевые слова: открыть большой JSON файл, просмотрщик больших JSON, открыть JSON 2 ГБ, открыть JSON 3 ГБ, JSON файл слишком большой, JSON просмотрщик для Mac, бесплатная альтернатива Dadroit, JSON вьюер с открытым исходным кодом, поиск в большом JSON, гигабайтный JSON, потоковый JSON просмотрщик.</sub>
